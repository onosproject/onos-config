nameOverride: ""

replicas: 1
annotations: {}

image:
  repository: onosproject/device-simulator
  tag: latest
  pullPolicy: IfNotPresent
  pullSecrets: []

resources:
  requests:
    cpu: 0.5
    memory: 512Mi

service:
  type: ClusterIP